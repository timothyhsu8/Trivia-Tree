import React 			from 'react';
import { useQuery } 	from '@apollo/client';
import * as queries 	from './cache/queries';
import { BrowserRouter, Switch, Route, Redirect, Router } from 'react-router-dom';

 
const App = (props) => {

	return(
		<div>Welcome to Trivia Tree</div>
	);
}

export default App;